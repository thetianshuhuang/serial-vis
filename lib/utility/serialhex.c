// Converts hex string to float or int.

// takes in empty float and char*, outputs full float
void hex_to_float(char * input, void * infloat) {
	
	int chars;
	if(sizeof(infloat) = 4) {
		chars = 8;
	}
	else if(sizeof(infloat) == 8) {
		chars = 16;
	}
	else {
		chars = 0;
	}

	while(chars > 0) {
		
	}
}